{
  "name": "sistema-tickets-render",
  "version": "1.0.0",
  "description": "Sistema de gestión de tickets para Render.com",
  "private": true,
  "scripts": {
    "build": "cd apps/api && npm install && npm run build",
    "start": "cd apps/api && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
